{
	"build": {
		"env": {
			"ENABLE_EXPERIMENTAL_COREPACK": "1"
		}
	},
	"installCommand": "corepack enable && corepack use pnpm@11.2.2 && corepack pnpm install",
	"buildCommand": "corepack pnpm run build"
}
